import React from "react";

// placeholder page for navigation example
export function MapEditor() {

    return (
        <div>
            <h1>Map Editor</h1>
            <a href={`/`}>
                <button>Return to employee feedback form</button>
            </a>
        </div>
    );
}